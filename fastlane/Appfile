app_identifier("co.amalgamated.ComeOnBackPager")
team_id("9XKN747L88") # Apple Developer Portal team (also used for signing)

# Auth for App Store Connect is via API key (see Fastfile / app_store_connect_api_key),
# so no apple_id / itc_team_id is needed here.
